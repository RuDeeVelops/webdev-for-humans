/* Let's suppose you have a header menu bar with three elements: a logo, a title and a description.
You might wanna have the logo and title on the left and description on the right.
A quick way to achieve this is to use flexbox and margin auto on one of the elements.
*/

.header {
  display: flex;
  justify-content: space-between;
}

.logo {
  width: 50px;
  height: 50px;
}

.title {
  font-size: 24px;
}

.description {
  margin-left: auto; /* This will push the description to the right */
}
