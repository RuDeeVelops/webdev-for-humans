// In React, attributes are indicated in slightly different ways than in HTML. For example, class becomes className in JSX and onclick becomes onClick.
// Example:

<button className="myButton" onClick={whateverFunction}>
  Click Me
</button>;
