// The join method in JavaScript converts an array into a string, and it allows you to specify the separator that goes between the elements in the resulting string. Example:
const arr = ["a", "b", "c"];
const str = arr.join(", "); // 'a, b, c'
// We are "replacing" the default separator (a comma) with a comma and a space.
